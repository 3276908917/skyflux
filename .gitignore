
*.DS_Store

# Python byte-code is by definition optimized for the LOCAL machine. It's no good for distribution.
*.pyc

# checkpoints are good for local users to undo mistakes,
# but version control (i.e. Git) already handles mistakes for
# the repository as a whole.
.ipynb_checkpoints

# junk generated by install
build
flux.egg-info
dist

# materials used for one-time model generation. Not intended for client use.
patterns
